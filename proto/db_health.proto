syntax = "proto3";

package db_health;

service DbHealthService {
  rpc checkTableHealth(CheckTableHealthRequest) returns (CheckTableHealthResponse);
}

message CheckTableHealthRequest {
  string table_name = 1;
}

message CheckTableHealthResponse {
  bool healthy = 1;
  string message = 2;
}